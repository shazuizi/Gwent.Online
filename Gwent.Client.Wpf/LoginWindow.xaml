<Window x:Class="Gwent.Client.Wpf.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gwint Online - Logowanie" Height="280" Width="420"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Nick -->
        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center">Nick:</TextBlock>
        <TextBox x:Name="TxtNick" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5"/>

        <!-- Tryb -->
        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center">Tryb:</TextBlock>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,5">
            <RadioButton x:Name="RbHost" Content="Hostuj" IsChecked="True" Margin="0,0,10,0" GroupName="Mode"/>
            <RadioButton x:Name="RbJoin" Content="Dołącz" GroupName="Mode"/>
        </StackPanel>

        <!-- Adres -->
        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center">Adres serwera:</TextBlock>
        <TextBox x:Name="TxtAddress" Grid.Row="2" Grid.Column="1" Margin="0,0,0,5" Text="127.0.0.1"/>

        <!-- Port -->
        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center">Port:</TextBlock>
        <TextBox x:Name="TxtPort" Grid.Row="3" Grid.Column="1" Margin="0,0,0,5" Text="9000"/>

        <!-- Przyciski -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="80" Margin="0,0,10,0" Click="Ok_Click"/>
            <Button Content="Anuluj" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
